{
  "name": "ai-news-bot-worker",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/index.js && node --check src/utils.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv namespace create CHAT_HISTORY",
    "secret:tg": "wrangler secret put TELEGRAM_BOT_TOKEN",
    "secret:webhook": "wrangler secret put TELEGRAM_WEBHOOK_SECRET",
    "secret:deepseek": "wrangler secret put DEEPSEEK_API_KEY",
    "secret:github": "wrangler secret put GITHUB_TOKEN"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
